import { defineConfig } from 'vite'

export default defineConfig({
  // Relative URLs let the same build run at a domain root, under a GitHub
  // Pages repository path, and from any ordinary static HTTP server.
  base: './',
})
